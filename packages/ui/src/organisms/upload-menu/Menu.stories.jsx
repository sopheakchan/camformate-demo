import { UploadMenu } from './Menu'

export default {
  title: 'Organisms/Upload',
  component: UploadMenu,
}

const Template = args => <UploadMenu {...args} />

export const Menu = Template.bind({})
